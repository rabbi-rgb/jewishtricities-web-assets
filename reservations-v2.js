(function(){var d=document;if(d.getElementById("tc-hh-reservations-ux-v2"))return;var s=d.createElement("style");s.id="tc-hh-reservations-ux-v2";s.textContent="html.tc-hh-reservations-final{--tc-hh-maroon:#a71938;--tc-hh-maroon-dark:#7e1029;--tc-hh-gold:#b58a45;--tc-hh-gold-soft:#e7d6ba;--tc-hh-ink:#2e2a25;--tc-hh-muted:#69635d;--tc-hh-line:#e7ded4;--tc-hh-ivory:#fbf8f3;--tc-hh-rose:#fff4f6} html.tc-hh-reservations-final #header a.parent[href=\"/4970020\"]{background:var(--tc-hh-maroon-dark) !important;border-color:var(--tc-hh-maroon-dark) !important} html.tc-hh-reservations-final #header a.parent[href=\"/4970020\"]:hover,html.tc-hh-reservations-final #header a.parent[href=\"/4970020\"]:focus{background:var(--tc-hh-maroon) !important} html.tc-hh-reservations-final #formContainer{background:linear-gradient(180deg,#ffffff 0%,#fdfaf6 34%,#ffffff 100%) !important} html.tc-hh-reservations-final #tc-hh-mobile-page-title{display:none} html.tc-hh-reservations-final #formContainer form[id=\"7431739\"] .tc-hh-progress-line{position:sticky !important;top:0 !important;z-index:80 !important;margin:0 !important;padding:8px 0 10px !important;background:rgba(255,255,255,0.94) !important;-webkit-backdrop-filter:blur(10px);backdrop-filter:blur(10px)} html.tc-hh-reservations-final #tc-hh-form-progress{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:7px;width:calc(100% - 40px);max-width:806px;margin:0 auto;padding:7px;border:1px solid var(--tc-hh-line);border-radius:15px;background:#ffffff;box-shadow:0 7px 24px rgba(70,47,37,0.08);box-sizing:border-box} html.tc-hh-reservations-final #tc-hh-form-progress a{display:flex;align-items:center;justify-content:center;gap:7px;min-width:0;padding:7px 8px;border-radius:10px;color:var(--tc-hh-muted);font-family:Muli,Arial,sans-serif;font-size:11px;font-weight:800;line-height:1.2;text-decoration:none !important;transition:color 160ms ease,background-color 160ms ease} html.tc-hh-reservations-final #tc-hh-form-progress a > span{display:inline-flex;flex:0 0 24px;align-items:center;justify-content:center;width:24px;height:24px;border:1px solid var(--tc-hh-gold-soft);border-radius:50%;background:var(--tc-hh-ivory);color:var(--tc-hh-maroon-dark);box-sizing:border-box} html.tc-hh-reservations-final #tc-hh-form-progress a.is-active{background:var(--tc-hh-rose);color:var(--tc-hh-maroon-dark)} html.tc-hh-reservations-final #tc-hh-form-progress a.is-active > span{border-color:var(--tc-hh-maroon);background:var(--tc-hh-maroon);color:#ffffff} html.tc-hh-reservations-final #tc-hh-form-progress a.is-complete > span{border-color:var(--tc-hh-gold);background:var(--tc-hh-gold);color:#ffffff} html.tc-hh-reservations-final #formContainer form[id=\"7431739\"]:is(#hh-step-1,#hh-step-2,#hh-step-3,#hh-step-4){max-width:840px !important;margin-top:34px !important;margin-bottom:12px !important;padding:17px 20px !important;border:1px solid var(--tc-hh-line) !important;border-left:4px solid var(--tc-hh-gold) !important;border-radius:16px !important;background:linear-gradient(135deg,#ffffff 0%,var(--tc-hh-ivory) 100%) !important;box-shadow:0 8px 24px rgba(70,47,37,0.06) !important} html.tc-hh-reservations-final #formContainer form[id=\"7431739\"]:is(#hh-step-1,#hh-step-2,#hh-step-3,#hh-step-4){scroll-margin-top:72px} html.tc-hh-reservations-final #formContainer form[id=\"7431739\"] #hh-step-4 > table:nth-child(2){overflow:hidden;border-radius:12px} html.tc-hh-reservations-final #formContainer form[id=\"7431739\"]:is(#id_35,#id_54,#id_57) .form-checkbox-item{border-color:var(--tc-hh-gold-soft) !important;background:var(--tc-hh-ivory) !important} html.tc-hh-reservations-final #formContainer form[id=\"7431739\"]:is(.form-checkbox-item,.form-radio-item){transition:border-color 150ms ease,background-color 150ms ease,box-shadow 150ms ease,transform 150ms ease !important} html.tc-hh-reservations-final #formContainer form[id=\"7431739\"]:is(.form-checkbox-item,.form-radio-item):hover{border-color:#d1b988 !important;box-shadow:0 5px 14px rgba(74,49,38,0.07) !important} html.tc-hh-reservations-final #formContainer form[id=\"7431739\"]:is(.form-checkbox-item,.form-radio-item).is-selected,html.tc-hh-reservations-final #formContainer form[id=\"7431739\"]:is(.form-checkbox-item,.form-radio-item):has(input:checked){border-color:var(--tc-hh-maroon) !important;background:var(--tc-hh-rose) !important;box-shadow:0 0 0 2px rgba(167,25,56,0.10),0 7px 18px rgba(74,35,43,0.08) !important} html.tc-hh-reservations-final .tc-hh-service-label{display:flex;align-items:center;gap:11px;width:100%;min-width:0} html.tc-hh-reservations-final .tc-hh-service-time{display:inline-flex;flex:0 0 auto;align-items:center;justify-content:center;min-width:67px;padding:4px 8px;border-radius:999px;background:#f1e4cf;color:var(--tc-hh-maroon-dark);font-size:11px;font-weight:900;line-height:1.25;letter-spacing:0.15px;white-space:nowrap;box-sizing:border-box} html.tc-hh-reservations-final .tc-hh-service-name{min-width:0;color:var(--tc-hh-ink);line-height:1.4} @media screen and (max-width:600px){html.tc-hh-reservations-final .cs-mobile-menu-open,html.tc-hh-reservations-final .cs-mobile-menu-open .fa{color:var(--tc-hh-maroon) !important} html.tc-hh-reservations-final #hh-form-intro{padding-top:22px !important} html.tc-hh-reservations-final #tc-hh-mobile-page-title{display:block;margin:0 0 26px;padding:0 0 18px;border-bottom:1px solid var(--tc-hh-line)} html.tc-hh-reservations-final #tc-hh-mobile-page-title span,html.tc-hh-reservations-final #tc-hh-mobile-page-title strong{display:block} html.tc-hh-reservations-final #tc-hh-mobile-page-title span{margin-bottom:4px;color:var(--tc-hh-gold);font:900 10px/1.3 Muli,Arial,sans-serif;letter-spacing:1.35px;text-transform:uppercase} html.tc-hh-reservations-final #tc-hh-mobile-page-title strong{color:var(--tc-hh-maroon-dark);font:normal 32px/1.05 Georgia,\"Times New Roman\",serif;letter-spacing:-0.4px} html.tc-hh-reservations-final #formContainer form[id=\"7431739\"] .tc-hh-progress-line{padding:6px 0 8px !important} html.tc-hh-reservations-final #tc-hh-form-progress{width:calc(100% - 20px);gap:3px;padding:5px;border-radius:13px} html.tc-hh-reservations-final #tc-hh-form-progress a{flex-direction:column;gap:3px;padding:5px 2px;font-size:9px;text-align:center} html.tc-hh-reservations-final #tc-hh-form-progress a > span{flex-basis:22px;width:22px;height:22px;font-size:10px} html.tc-hh-reservations-final #formContainer form[id=\"7431739\"]:is(#hh-step-1,#hh-step-2,#hh-step-3,#hh-step-4){width:calc(100% - 4px) !important;padding:14px 12px !important;border-radius:14px !important} html.tc-hh-reservations-final #formContainer form[id=\"7431739\"]:is(#hh-step-1,#hh-step-2,#hh-step-3,#hh-step-4) h2{font-size:25px !important} html.tc-hh-reservations-final .tc-hh-service-label{align-items:flex-start;gap:9px} html.tc-hh-reservations-final .tc-hh-service-time{min-width:64px;margin-top:-1px;padding:4px 7px;font-size:10px} }@media (prefers-reduced-motion:reduce){html.tc-hh-reservations-final #formContainer form[id=\"7431739\"]:is(.form-checkbox-item,.form-radio-item),html.tc-hh-reservations-final #tc-hh-form-progress a{transition:none !important} }\nhtml.tc-hh-reservations-final #tc-hh-start-reservation-wrap{display:none !important} html.tc-hh-reservations-final table.tc-hh-day-card{border:0 !important;border-left:0 !important;border-collapse:separate !important;border-radius:14px !important;background:linear-gradient(135deg,#fffaf4 0%,#fbf6ee 100%) !important;box-shadow:inset 0 0 0 1px rgba(181,138,69,.26),0 8px 24px rgba(54,38,35,.06) !important;overflow:hidden !important} html.tc-hh-reservations-final table.tc-hh-day-card td{padding:16px 18px !important;border-left:4px solid var(--tc-hh-gold) !important} html.tc-hh-reservations-final .tc-hh-day-title{margin:0 !important;color:var(--tc-hh-maroon-dark) !important;font-family:Georgia,\"Times New Roman\",serif !important;font-size:24px !important;line-height:1.15 !important;font-weight:normal !important;letter-spacing:-.2px !important} html.tc-hh-reservations-final .tc-hh-day-date{display:inline-block !important;margin:8px 0 0 !important;padding:5px 9px !important;border:1px solid rgba(181,138,69,.24) !important;border-radius:999px !important;background:#fff !important;color:var(--tc-hh-maroon-dark) !important;font-family:Muli,Arial,sans-serif !important;font-size:11px !important;line-height:1.25 !important;font-weight:900 !important;letter-spacing:.55px !important;text-transform:uppercase !important} @media screen and (max-width:600px){html.tc-hh-reservations-final table.tc-hh-day-card{border-radius:12px !important} html.tc-hh-reservations-final table.tc-hh-day-card td{padding:14px 15px !important} html.tc-hh-reservations-final .tc-hh-day-title{font-size:21px !important} html.tc-hh-reservations-final .tc-hh-day-date{font-size:10px !important;padding:4px 8px !important}}";d.head.appendChild(s);}());
(function () {
  var path = window.location.pathname || "";
  var search = window.location.search || "";
  var isReservationsPage =
    /\/aid\/7431739(?:\/|$)/.test(path) ||
    /(?:^|[?&])aid=7431739(?:&|$)/.test(search);
  if (!isReservationsPage) return;

  var formSelector = '#formContainer form[id="7431739"]';

  function addMobileTitle() {
    var inner = document.querySelector("#hh-form-intro .hh-bridge-inner");
    if (!inner) return false;
    if (document.getElementById("tc-hh-mobile-page-title")) return true;

    var title = document.createElement("div");
    title.id = "tc-hh-mobile-page-title";
    title.setAttribute("aria-label", "High Holiday Reservations");
    title.innerHTML = "<span>High Holidays</span><strong>Reservations</strong>";
    inner.insertBefore(title, inner.firstChild);
    return true;
  }

  function labelMobileMenu() {
    var control = document.getElementById("mobile-menu-expand");
    if (!control) return false;

    if (!control.querySelector(".tc-hh-menu-label")) {
      var label = document.createElement("span");
      label.className = "tc-hh-menu-label";
      label.textContent = "High Holidays menu";
      control.insertBefore(label, control.firstChild);
    }

    control.setAttribute("role", "button");
    control.setAttribute("tabindex", "0");
    control.setAttribute("aria-label", "Open or close the High Holidays menu");

    if (!control.hasAttribute("data-tc-hh-keyboard")) {
      control.setAttribute("data-tc-hh-keyboard", "true");
      control.addEventListener("keydown", function (event) {
        if (event.key === "Enter" || event.key === " ") {
          event.preventDefault();
          control.click();
        }
      });
    }
    return true;
  }

  function tidyServiceText(text, time) {
    var name = text.replace(time, "");
    name = name.replace(/\s*[•–—-]\s*/g, " · ");
    name = name.replace(/\s*,\s*/g, " · ");
    name = name.replace(/(?:\s*·\s*)+/g, " · ");
    name = name.replace(/^\s*·\s*|\s*·\s*$/g, "");
    return name.trim();
  }

  function enhanceServiceLabels() {
    var cards = document.querySelectorAll(
      formSelector + " :is(#id_51, #id_44, #id_39) .form-checkbox-item"
    );
    if (!cards.length) return false;

    cards.forEach(function (card) {
      var input = card.querySelector('input[type="checkbox"]');
      var textNode = card.querySelector("label > span");
      if (!input || !textNode) return;

      if (!textNode.hasAttribute("data-tc-hh-service")) {
        var original = textNode.textContent.trim();
        var match = original.match(/\b\d{1,2}:\d{2}\s*(?:AM|PM)\b/i);
        textNode.textContent = "";
        textNode.className = "tc-hh-service-label";
        textNode.setAttribute("data-tc-hh-service", "true");

        if (match) {
          var time = document.createElement("span");
          time.className = "tc-hh-service-time";
          time.textContent = match[0].toUpperCase();

          var name = document.createElement("span");
          name.className = "tc-hh-service-name";
          name.textContent = tidyServiceText(original, match[0]);

          textNode.appendChild(time);
          textNode.appendChild(name);
        } else {
          textNode.classList.add("tc-hh-service-only");
          var onlyName = document.createElement("span");
          onlyName.className = "tc-hh-service-name";
          onlyName.textContent = original;
          textNode.appendChild(onlyName);
        }
      }
    });
    return true;
  }

  function syncSelectedCards() {
    var controls = document.querySelectorAll(
      formSelector + ' .form-checkbox-item input[type="checkbox"],' +
      formSelector + ' .form-radio-item input[type="radio"]'
    );
    if (!controls.length) return false;

    controls.forEach(function (input) {
      var card = input.closest(".form-checkbox-item, .form-radio-item");
      if (card) card.classList.toggle("is-selected", input.checked);

      if (!input.hasAttribute("data-tc-hh-selected-listener")) {
        input.setAttribute("data-tc-hh-selected-listener", "true");
        input.addEventListener("change", syncSelectedCards);
      }
    });
    return true;
  }

  function updateProgress() {
    var progress = document.getElementById("tc-hh-form-progress");
    if (!progress) return;

    var current = 1;
    var marker = window.pageYOffset + 150;
    for (var i = 1; i <= 4; i += 1) {
      var step = document.getElementById("hh-step-" + i);
      if (step && step.getBoundingClientRect().top + window.pageYOffset <= marker) {
        current = i;
      }
    }

    progress.querySelectorAll("a[data-step]").forEach(function (link) {
      var stepNumber = Number(link.getAttribute("data-step"));
      link.classList.toggle("is-active", stepNumber === current);
      link.classList.toggle("is-complete", stepNumber < current);
      if (stepNumber === current) {
        link.setAttribute("aria-current", "step");
      } else {
        link.removeAttribute("aria-current");
      }
    });
  }

  function addProgress() {
    var firstStep = document.getElementById("hh-step-1");
    if (!firstStep) return false;
    if (document.getElementById("tc-hh-form-progress")) {
      updateProgress();
      return true;
    }

    var firstLine = firstStep.closest(".form-line");
    if (!firstLine || !firstLine.parentNode) return false;

    var line = document.createElement("li");
    line.className = "form-line tc-hh-progress-line";

    var progress = document.createElement("nav");
    progress.id = "tc-hh-form-progress";
    progress.setAttribute("aria-label", "Reservation progress");

    var labels = ["Guests", "Services", "Contact", "Support"];
    labels.forEach(function (label, index) {
      var stepNumber = index + 1;
      var link = document.createElement("a");
      link.href = "#hh-step-" + stepNumber;
      link.setAttribute("data-step", String(stepNumber));
      link.innerHTML =
        "<span>" + stepNumber + "</span><strong>" + label + "</strong>";
      link.addEventListener("click", function (event) {
        var target = document.getElementById("hh-step-" + stepNumber);
        if (!target) return;
        event.preventDefault();
        var reduced = window.matchMedia &&
          window.matchMedia("(prefers-reduced-motion: reduce)").matches;
        window.scrollTo({
          top: window.pageYOffset + target.getBoundingClientRect().top - 68,
          behavior: reduced ? "auto" : "smooth"
        });
      });
      progress.appendChild(link);
    });

    line.appendChild(progress);
    firstLine.parentNode.insertBefore(line, firstLine);

    if (!document.documentElement.hasAttribute("data-tc-hh-progress-listener")) {
      document.documentElement.setAttribute("data-tc-hh-progress-listener", "true");
      window.addEventListener("scroll", updateProgress, { passive: true });
      window.addEventListener("resize", updateProgress);
    }
    updateProgress();
    return true;
  }

  function defaultEmailOptInOff() {
    var optIn = document.querySelector(formSelector + ' input[name="optin"]');
    if (!optIn) return false;
    if (!optIn.hasAttribute("data-tc-hh-optin-default")) {
      optIn.checked = false;
      optIn.defaultChecked = false;
      optIn.removeAttribute("checked");
      optIn.setAttribute("data-tc-hh-optin-default", "off");
      optIn.dispatchEvent(new Event("change", { bubbles: true }));
    }
    return true;
  }

  function stylePrivacyButton() {
    var pill = document.getElementById("echo-cmp-pill");
    if (!pill) return false;

    var mobile = window.matchMedia &&
      window.matchMedia("(max-width: 600px)").matches;
    var base = "rgba(126, 16, 41, 0.96)";
    var hover = "rgba(167, 25, 56, 0.98)";

    pill.style.setProperty("background", base, "important");
    pill.style.setProperty("border", "1px solid rgba(255,255,255,0.24)", "important");
    pill.style.setProperty("box-shadow", "0 5px 16px rgba(74,35,43,0.20)", "important");
    pill.style.setProperty("bottom", "10px", "important");
    pill.style.setProperty("left", "10px", "important");

    var spans = pill.querySelectorAll("span");
    if (mobile) {
      pill.style.setProperty("width", "42px", "important");
      pill.style.setProperty("height", "42px", "important");
      pill.style.setProperty("padding", "0", "important");
      pill.style.setProperty("gap", "0", "important");
      pill.style.setProperty("justify-content", "center", "important");
      spans.forEach(function (span) {
        span.style.setProperty("display", "none", "important");
      });
    } else {
      pill.style.removeProperty("width");
      pill.style.removeProperty("height");
      pill.style.setProperty("padding", "7px 12px", "important");
      pill.style.setProperty("gap", "6px", "important");
      pill.style.removeProperty("justify-content");
      spans.forEach(function (span) {
        span.style.removeProperty("display");
      });
    }

    if (!pill.hasAttribute("data-tc-hh-burgundy")) {
      pill.setAttribute("data-tc-hh-burgundy", "true");
      pill.addEventListener("mouseenter", function () {
        pill.style.setProperty("background", hover, "important");
      });
      pill.addEventListener("mouseleave", function () {
        pill.style.setProperty("background", base, "important");
      });
    }
    return true;
  }


  function enhanceDayHeaders() {
    var matched = 0;
    document.querySelectorAll(formSelector + " h3").forEach(function (heading) {
      var text = (heading.textContent || "").trim();
      if (!/^(?:Rosh Hashanah, Day [12]|Yom Kippur)$/.test(text)) return;
      heading.classList.add("tc-hh-day-title");
      var table = heading.closest("table");
      if (table) table.classList.add("tc-hh-day-card");
      var date = heading.nextElementSibling;
      if (date && date.tagName === "P") date.classList.add("tc-hh-day-date");
      matched += 1;
    });
    return matched > 0;
  }

  function applyEnhancements() {
    return {
      title: addMobileTitle(),
      dayHeaders: enhanceDayHeaders(),
      services: enhanceServiceLabels(),
      selected: syncSelectedCards(),
      progress: addProgress(),
      optIn: defaultEmailOptInOff(),
      privacy: stylePrivacyButton()
    };
  }

  function begin() {
    applyEnhancements();

    var observer = new MutationObserver(function () {
      applyEnhancements();
    });
    observer.observe(document.documentElement, { childList: true, subtree: true });

    window.setTimeout(function () {
      observer.disconnect();
      applyEnhancements();
    }, 15000);
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", begin, { once: true });
  } else {
    begin();
  }
})();
